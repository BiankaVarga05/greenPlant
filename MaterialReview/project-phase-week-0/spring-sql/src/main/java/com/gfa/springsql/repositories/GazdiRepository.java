package com.gfa.springsql.repositories;

import com.gfa.springsql.models.Gazdi;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GazdiRepository extends JpaRepository<Gazdi, Long> {
}
